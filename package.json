{
  "name": "punk-hot-ai-feed",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4173",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-4173}",
    "fetch:sources": "node scripts/fetch-feeds.mjs",
    "verify:sources": "node scripts/verify-sources.mjs",
    "test": "playwright test",
    "score": "node scripts/score-project.mjs"
  },
  "dependencies": {
    "fast-xml-parser": "^5.3.2",
    "vite": "^7.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
